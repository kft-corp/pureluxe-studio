import { ShellModulePage } from "@/components/shell";

export default function SettingsPage() {
  return (
    <ShellModulePage
      module="settings"
      title="Settings"
      description="Branding, rate sources, and studio configuration."
    />
  );
}
