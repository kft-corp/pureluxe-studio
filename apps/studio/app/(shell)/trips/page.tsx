import { ShellModulePage } from "@/components/shell";

export default function TripsPage() {
  return (
    <ShellModulePage
      module="trips"
      title="Trips"
      description="Active, upcoming, and past trips — open any trip in Trip Builder."
    />
  );
}
