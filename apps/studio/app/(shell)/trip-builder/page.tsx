import { ShellModulePage } from "@/components/shell";

export default function TripBuilderPage() {
  return (
    <ShellModulePage
      module="trip_builder"
      title="Trip Builder"
      description="Chat, itinerary, rates, and proposals — your primary daily workspace."
    />
  );
}
