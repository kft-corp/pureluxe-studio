import { ShellModulePage } from "@/components/shell";

export default function BookingsPage() {
  return (
    <ShellModulePage
      module="bookings"
      title="Bookings"
      description="Search bookings, filter by status, and open detail panels."
    />
  );
}
