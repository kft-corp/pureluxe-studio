import { ShellModulePage } from "@/components/shell";

export default function TeamPage() {
  return (
    <ShellModulePage
      module="team"
      title="Team & Roles"
      description="Manage members, invites, and role permissions."
    />
  );
}
