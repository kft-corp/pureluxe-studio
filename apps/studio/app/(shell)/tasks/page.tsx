import { ShellModulePage } from "@/components/shell";

export default function TasksPage() {
  return (
    <ShellModulePage
      module="tasks"
      title="Tasks"
      description="Team tasks, follow-ups, and operational checklists."
    />
  );
}
