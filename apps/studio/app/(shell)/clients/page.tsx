import { ShellModulePage } from "@/components/shell";

export default function ClientsPage() {
  return (
    <ShellModulePage
      module="clients"
      title="Clients"
      description="Client list, profiles, family links, and starting new trips."
    />
  );
}
