/** Encrypted Studio session cookie — keep in sync across Edge middleware and Node routes. */
export const STUDIO_SESSION_COOKIE_NAME = "pureluxe_studio_session";

/** Minimum length enforced for STUDIO_SESSION_SECRET (Edge + Node). */
export const STUDIO_SESSION_SECRET_MIN_LENGTH = 32;
